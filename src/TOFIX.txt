/get_id
...
1.
    to get_id to pairnei san params
    allagi
    json={"public_key": wallet.public_key, "ip": ip, "port": port} 
    as params
    ...
    auto egine FIX alla ta nodes synexizoun na min xtypane to get_id
    CONNECTIVITY metaksi twn containers 
    FIX (???) : docker networking 
    FIXED : ta containers tou docker epikoinwnoun me --name oxi me to loopback address 127.0.0.1
            prepei na elenksoume an auto ephreazei to programma 
            pleon ta ip-adress einai container{i}

2.
    exei auta ta orismata sto get_id : 
    node.add_node(node_id, public_key, ip, port) 
    sto definition exei : 
    def add_node(self, node_id: int, ip: str, port: int, wallet: Wallet):
    ti kanoume me to wallet ??? pws to dinoume sto request ??
    -> swsto htan, ksexasa na ftiaxnw to wallet sthn get_id, pairnei wallet, den xreiazetai h generate_boot_wallet

    ~ einai komple na pairnei o bootsrap olo to wallet ???
    diladi kai private_key allou kombou ???
    -> oxi pairnei mono to public key swsta

    candidate FIX :
    a. i add_node() pairnei wallet xwris to private_key
        prin apo tin add_node() o bootstrap kanei ena 
        generate_new_wallet_boot pou den orizei private_key
        dld dimouirgei antigrafo tou wallet kapoiou kombou
        def generate_new_wallet_boot(public_key: str, ip: str, port: int) -> Wallet:
            private_key_str = -1
            public_key_str = public_key
            return Wallet(public_key_str, private_key_str)

    b. i add_node() den pairnei wallet doulevei mono me 
        broadcaster(), prepei na allaksei kai to node.nodes


3. 
    otan kanoume yield() pairnoume:
    TypeError: Object of type generator is not JSON serializable
    FIX : bazoume attribute gen_id sto node kai to auksanoume

4. 
    stin add_node()
    ...
    self.nodes[public_key] = wallet -> self.nodes[ip] = wallet
    -> ^^^ auto einai lathos h self.nodes prepei na douleuei me public_key
    ~~~~~~~~~~~~~~~
    /balance
    to address menei panta keno opote :
    api.py ln 27 +++ node.address = ip
    FIXED
    to address kai to public key ta xreiazomaste gia to diktyo
    -> address=public key, giati xreiazomaste thn ip kapou allou peran tou broadcaster?

5. receive_mapping()
    se kapoia to port to exoume int kai se alla einai int 
    prepei na exoun ola to idio 
    proteinw str
    FIXED

    den mporoun o bootstrap na kanei auto to request stous containers
    fainetai gia networking problhma

6. broadcaster.broadcast_mapping()
    requests.post(url, json=self.nodes, timeout=10)
    apo oti katalava den xreiazetai na kanoume to dict -> json
    to pairnei sto body kai to metatrepei mono tou
    -> Ara fixed?


    epishs o bootstrap prepei na kanei broadcast_mapping() ston eauto tou ??
    -> Oxi prepei na afairoume sta broadcast thn ip aftou pou kanei broadcast me ena continue